// hello = () => alert('Hello World!');

// hello = (firstName, lastName) =>
// {
//     alert('Hello' + firstName + ' ' + lastName);
// }

const Header = () => {
    return (
        <header>
            <h1 className="mt- p5 bg-dark text-white rounded">Header</h1>
        </header>
    )
}
export default Header;